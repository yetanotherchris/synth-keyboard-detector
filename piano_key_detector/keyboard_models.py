"""Simplified keyboard layout utilities - no longer needed for first white key detection."""
# This file is kept for backwards compatibility but is no longer actively used
# since the detector now only finds the first white key instead of full keyboard layouts

from __future__ import annotations

# Minimal stub to prevent import errors
def generate_black_key_pattern(num_white_keys: int) -> list:
    """Stub function - no longer used."""
    return []
